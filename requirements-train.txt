# Training dependencies (for Colab/GPU training)
# 
# NOTE: Install PyTorch first with CUDA support:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#
# Then install this file:
#   pip install -r requirements-train.txt

--extra-index-url https://download.pytorch.org/whl/cu126
torch
torchvision
transformers>=4.40.0
trl>=0.8.0
peft>=0.10.0
accelerate>=0.28.0
bitsandbytes>=0.43.0
datasets>=2.18.0

# Also install base requirements
-r requirements.txt
